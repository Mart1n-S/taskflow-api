# Environment variables for TaskFlow API
NODE_ENV=test

# Database configuration
DB_HOST=localhost
DB_PORT=5433
DB_NAME=taskflow_test
DB_USER=taskflow
DB_PASSWORD=taskflow

# JWT configuration
JWT_SECRET=test-secret-key
JWT_EXPIRES_IN=1h

# Prisma (base de test dédiée si besoin)
DATABASE_URL="postgresql://taskflow:taskflow@localhost:5433/taskflow_prisma_test?schema=public"